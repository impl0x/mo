# Mo - api framework

A backend server template which is made on top of net/http and is lightweight

inspired heavily from echo

Everything done except
- Radix tree router (idk if ill be able to make this rn tho)

(current the default router is BasicRouter, O(n) lookup times)

